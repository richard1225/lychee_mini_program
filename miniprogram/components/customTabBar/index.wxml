<!--components/customTabBar/index.wxml-->

<view>

  <van-tabbar active="{{ pageIndex }}" bind:change="onChange">
    <van-tabbar-item wx:for="{{pageList}}" wx:key="unique" icon="{{item.icon}}">{{item.label}}</van-tabbar-item>
  </van-tabbar>
</view>